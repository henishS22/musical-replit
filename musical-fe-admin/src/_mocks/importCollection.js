const importCollection = [...Array(24)].map((_, index) => ({
  id: index,
  status: false,
  username: '@username',
  category: 'Art',
  address: '0x00514....6e2',
}));

export default importCollection;
